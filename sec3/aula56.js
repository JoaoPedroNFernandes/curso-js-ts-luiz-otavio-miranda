//for of
//for of pega o valor de objetos interaveis (array, map, set...) (não dá certo com objetos)

const pessoas = ['Ana', 'Maria', 'Joao', 'Pedro'];

//é tipo: valor de pessoas (ou seja, valor dd array)
for(let valor of pessoas) {
    console.log(valor)
}