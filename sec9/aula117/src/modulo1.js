export const nome = 'João';
export const sobrenome = 'Fernandes';
export const idade = 18;

//expoprt default só pode haver um por modulo e o import pega ele quando chamado sem chaves
export default function soma(x, y) {
    return x + y;
}